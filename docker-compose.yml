version: "3.9"

services:
  frontend:
    build:
      context: ./frontend-suno
    ports:
      - "3000:80"
    depends_on:
      - backend
  
  frontend-minsoo:
    build:
      context: ./frontend-minsoo
    ports:
      - "5173:80"
    depends_on:
      - backend

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    ports:
      - "8000:8000"
    environment:
      REDIS_HOST: redis
      REDIS_PORT: 6379
      REDIS_DB: 0
      GPT_API_KEY: ${GPT_API_KEY:-}
      SUNO_API_KEY: ${SUNO_API_KEY:-}
      SUNO_CALLBACK_URL: ${SUNO_CALLBACK_URL:-}
      SUNO_API_BASE: ${SUNO_API_BASE:-https://api.sunoapi.org}
      SUNO_MODEL: ${SUNO_MODEL:-V4_5ALL}
      SORA_API_KEY: ${SORA_API_KEY:-}
      SORA_API_BASE: ${SORA_API_BASE:-https://api.openai.com/v1}
      SORA_IMAGE_ENDPOINT: ${SORA_IMAGE_ENDPOINT:-/images/generations}
      MINIO_ENDPOINT: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      MINIO_SECURE: "false"
      MINIO_PUBLIC_ENDPOINT: ${MINIO_PUBLIC_ENDPOINT:-localhost:9000}
    depends_on:
      - redis
      - minio

  redis:
    image: redis:7.2-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"
      - "9001:9001"
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio-data:/data

volumes:
  redis-data:
  minio-data:
